
ALPHA_STATE = "Alpha"
BETA_STATE = "Beta"
PRE_RELEASE = "Pre"
RELEASE = ""

MAJOR_VERSION = 0
MINOR_VERSION = 1
PATCH_VERSION = 5
VERSION_STATE = ALPHA_STATE



versionStr = str(MAJOR_VERSION) + "." + str(MINOR_VERSION) + "." + str(PATCH_VERSION) + "-" + VERSION_STATE

#__version__ = str(MAJOR_VERSION) + "." + str(MINOR_VERSION) + "." + str(PATCH_VERSION) + "-" + VERSION_STATE

#print(__version__)
